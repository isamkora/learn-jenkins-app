pipeline {
    agent any

    stages {
        stage('Repo run') {
            steps {
                sh '''
                    echo "First run in a repo"
                '''
            }
        }
    }
}